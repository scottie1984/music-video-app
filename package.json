{
  "name": "music-video-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-scripts": "0.9.x",
    "react-spinners": "0.8.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "postbuild": "rm -rf docs && mv build docs",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "commit": "git add . && git commit -m 'Static site build' && git push",
    "publish": "npm run build && npm run commit"
  },
  "homepage": "."
}
